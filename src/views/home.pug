extends layouts/main.pug
include mixins/video-block.pug

block content
    .home-videos
        each item in videos
            +videoBlock({
                id: item.id,
                title: item.title,
                views: item.views,
                videoFile: item.fileUrl,
            })